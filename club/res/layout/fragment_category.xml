<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" 
    android:background="@color/custom_background_color">
      
    <GridView
        android:id="@+id/gridView_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/gridview_padding"
        android:layout_marginRight="@dimen/gridview_padding"
        android:gravity="center"
        android:horizontalSpacing="@dimen/gridview_horizontal_spacing"
        android:listSelector="@android:color/transparent"
        android:numColumns="2"
        android:scrollbarStyle="outsideOverlay"
        android:stretchMode="columnWidth"
        android:verticalSpacing="@dimen/category_gridview_vertical_spacing" 
        android:layout_marginTop="@dimen/category_margin_navigation">
    </GridView>
    
    <include
        android:id="@+id/search_loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        layout="@layout/progress_bar_intern"/>

    <include layout="@layout/empty_data"/>

</LinearLayout>