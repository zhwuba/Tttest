<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/icon"
        android:layout_width="match_parent"
        android:layout_height="@dimen/theme_preview_h"
        android:background="@drawable/theme_no_default"
        android:scaleType="fitXY" />

    <ImageView
        android:id="@+id/type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/icon"
        android:layout_alignTop="@id/icon"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/icon"
        android:layout_alignTop="@id/icon"
        android:layout_marginTop="@dimen/status_icon_margin_top"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/icon"
        android:layout_marginTop="7dp"
        android:gravity="center_vertical" >

        <TextView
            android:id="@+id/text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="3dp"
            android:layout_weight="1"
            android:singleLine="true"
            android:textColor="@color/primary_text_color"
            android:textSize="@dimen/theme_name_text_size" />

        <Button
            android:id="@+id/apply"
            android:layout_width="@dimen/theme_apply_w"
            android:layout_height="@dimen/theme_apply_h"
            android:layout_gravity="right"
            android:background="@drawable/tab_theme_apply"
            android:focusable="false" />
    </LinearLayout>

</RelativeLayout>