<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/common_bg" >

    <ListView
        style="@style/common_scroll_style"
        android:id="@+id/category_listview"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:cacheColorHint="#00000000"
        android:divider="@null"
        android:fadingEdge="none"
        android:listSelector="#00000000"
        android:scrollbars="horizontal"
        android:layout_marginLeft="@dimen/category_a_layout_margin"
    	android:layout_marginRight="@dimen/category_a_layout_margin"
        android:visibility="gone" >
    </ListView>

    <include
        android:id="@+id/search_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        layout="@layout/progress_bar_intern"
        android:visibility="gone" />
    
    <include
        android:id="@+id/refresh_linearLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        layout="@layout/layout_network_ungeilivable"
        android:visibility="gone" />

</RelativeLayout>